{% assign sorted = site.pages | sort:"url" %}
{% for node in sorted offset:1 %}
  {% capture PATH %}{{ node.url }}{% endcapture %}
  {{PATH}}
  {% assign slugs = node.url | split: '/' %}
  {% for slug in slugs %}
    {% capture SLUG %}{{ slug }}{% capture SLUG %}
    {{SLUG}}<br>
  {% endfor %}
{% endfor %}
