{% assign sorted = site.pages | sort:"url" %}
{% for node in sorted offset:1 %}{{node.url}}
  {% assign slugs = node.url | split: '/' %}
  {% for slug in slugs %}{% assign level = forloop.index %}{{ level }}{{slug}}{% endfor %}
  <br>
{% endfor %}
