{% assign sorted_pages = site.pages | sort:"url" %}

{% for node in sorted_pages offset:1 %}

  {{node.url}}<br>
  {{forloop.length}}<br>
  
  {% for item in node.url | split: '/' %}
  
    {{forloop.length}}<br>
    {{node.size}}<br>
  {% endfor %}
  
{% endfor %}
