{% capture _root_children %}
{% for node in site.pages %}
{% if page.url == site.baseurl and node.url | split: '/' | size == 1 %}
<li><a href='{{node.url}}'>{{node.url  | split: '/' | last }}</a></li>
{% endif %}
{% endfor %}
{% endcapture %}
